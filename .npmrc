# NPM configuration
save-exact=true
engine-strict=true

# Lock down versions for consistency
prefer-offline=true

# Avoid accidental publishing
access=restricted

# Clean output folder before build
scripts-prepend-node-path=true

# Don't save exactly to package.json
save-prefix=^ 